version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: qagent
      POSTGRES_PASSWORD: qagent_dev
      POSTGRES_DB: qagent_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U qagent"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
